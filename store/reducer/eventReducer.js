import EVENTS from "../../dummy-data/dummy-data";

const initialState = {
    events: EVENTS,
}

const eventReducer = (state,action) => {
    switch(action){
        default:
            return state;
    }
}