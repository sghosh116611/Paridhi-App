class Event{
    constructor(id,domainId,title,description,amount){
        this.id = id,
        this.domainId = domainId,
        this.title = title,
        this.description = description,
        this.amount = amount
    }
}

export default Event;