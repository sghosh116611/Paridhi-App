class Event{
    constructor(id,domainId,title,description,amount,imgUrl){
        this.id = id,
        this.domainId = domainId,
        this.title = title,
        this.description = description,
        this.amount = amount,
        this.imgUrl = imgUrl
    }
}

export default Event;